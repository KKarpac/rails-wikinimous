<div class='container'>
  <ul class='list-group'>
    <% @articles.each do |article| %>
      <li class='list-group-items'><%= link_to article.title, article_path(article) %>
        <p><%= article.content %></p>
      </li>
      <% end %>
  </ul>

<p>
  <%= link_to "+ Add Article", new_article_path %>
</p>
</div>
